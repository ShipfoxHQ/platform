extends: substitution
message: "Use '%s' instead of '%s'."
level: warning
ignorecase: true
capitalize: true
action:
  name: replace
swap:
  leverage: use
  dispatch: send
  utilize: use
  facilitate: help
  subsequent: later
  prior to: before
  all of: all
  pipeline: workflow
