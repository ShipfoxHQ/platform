---
"@shipfox/api-server": minor
---

Adds an `agentModule` factory option to `DefaultModulesOptions` so hosts can replace the standard Agent module while keeping the composed Secrets client and a single inter-module transport.
