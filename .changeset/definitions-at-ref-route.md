---
"@shipfox/api-definitions-dto": minor
"@shipfox/api-definitions": minor
---

Adds `GET /definitions/at-ref` listing workflow definitions at a git ref with their validation state and the pinned commit, backing the run-from-branch picker.
