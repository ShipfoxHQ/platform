---
"@shipfox/api-integration-core": minor
"@shipfox/api-integration-core-dto": minor
---

Adds the inter-module `callTool` method for server-executed integration tool calls, tagged with the caller on the audit line and the `integrations_agent_tool_call` metric.
