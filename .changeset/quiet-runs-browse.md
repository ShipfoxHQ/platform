---
"@shipfox/api-workflows": patch
"@shipfox/api-workflows-dto": minor
---

Add the workflow-run lineage head endpoint and opt-in cursor pagination for attempt history while preserving the legacy no-query response.
