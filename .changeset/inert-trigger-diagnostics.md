---
"@shipfox/api-definitions": patch
---

Makes trigger-scoped validation errors inert. Reports broken triggers as error diagnostics with their paths while the definition and its other triggers keep syncing.
