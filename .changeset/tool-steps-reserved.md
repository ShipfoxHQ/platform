---
"@shipfox/workflow-document": minor
---

Adds reserved tool step fields (`tool`, `connection`, `with`, and the expression `outputs` mapping form) to the workflow document schema, with `with` size and depth limits. The schema rejects steps that use them for now.
