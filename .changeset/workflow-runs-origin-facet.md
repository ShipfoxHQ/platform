---
"@shipfox/api-workflows-dto": minor
"@shipfox/api-workflows": minor
---

Adds `origin` and `dev_source` to workflow runs, with an `origin` facet on the run list and aggregates endpoints. Existing runs read as `synced`.
