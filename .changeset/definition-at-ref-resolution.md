---
"@shipfox/api-definitions-dto": major
"@shipfox/api-definitions": minor
---

Adds `resolveDefinitionAtRef` and `listDefinitionsAtRef` inter-module methods that resolve and validate workflow definitions at a git ref without persisting them.
