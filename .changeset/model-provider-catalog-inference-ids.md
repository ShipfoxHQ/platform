---
"@shipfox/api-agent-dto": minor
"@shipfox/api-agent": patch
"@shipfox/client-agent": minor
---

Adds `managed_provider_id` and `instance_default_provider_id` to the model-provider catalog response so clients can detect when an installation provides inference and omit redundant provider setup.
