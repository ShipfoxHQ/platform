---
"@shipfox/api-definitions": minor
---

Adds authoring checks for shared agent sessions: parallel resume on statically identical session keys across unordered jobs, and literal harness disagreement between steps sharing a session key.
