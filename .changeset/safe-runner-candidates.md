---
"@shipfox/api-runners": minor
"@shipfox/api-runners-dto": minor
---

Adds a backward-compatible provider termination-candidate protocol with durable, state-checked authorization.

Deploy the backend before enabling candidate reporting in provisioners. Disable candidate reporting before rolling back the backend.
