---
"@shipfox/api-triggers": minor
"@shipfox/api-triggers-dto": minor
---

Adds `origin` and `replayable` filters to the trigger events list, an `origins` facet, and `replay_of_event_id` with a `replays` list on the event detail response.
