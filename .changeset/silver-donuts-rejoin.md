---
"@shipfox/client-workspace-settings": minor
---

Exports the membership and invitation query options (and their query keys) from the package surface, and adds a "Show the setup guide" re-entry link to the general settings page that clears the shell-owned setup-checklist dismissal flag.
