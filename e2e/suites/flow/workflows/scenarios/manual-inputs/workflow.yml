name: Manual inputs
runner: __RUNNER_LABEL__
triggers:
  manual:
    source: manual
jobs:
  build:
    steps:
      - key: show-version
        env:
          V: '${{ inputs.version }}'
        run: echo "v=$V"
